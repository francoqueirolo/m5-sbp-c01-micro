## Microservices con Docker Compose

### Verificar que el Docker Desktop este ejecutandose

### Desde la carpeta donde esta el archivo docker-compose.yml
```
docker-compose up -d
```

### Verificar que están corriendo
```
docker ps
```
